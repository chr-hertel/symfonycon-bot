<strong>Results for "{{ query }}"</strong>

{# @var \App\Entity\Talk talk #}
{% for talk in talks %}
 • <strong>{{ talk.title }}</strong>
   {{ talk.timeSpan.toString }}
   » /talk@{{ talk.id|short }}

{% else %}
    Nothing found!
{% endfor %}
